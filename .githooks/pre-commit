#!/bin/sh
# Block commits that look like ERCOT data or participant identifiers.
# Enable once per clone: git config core.hooksPath .githooks
exec python3 "$(git rev-parse --show-toplevel)/tools/check_confidential.py" --staged
